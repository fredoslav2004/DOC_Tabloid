# Local running

Use the VS Code task `Run VIA Tabloid locally`, or run:

```bash
docker compose up --build
```

Open:

```text
http://localhost:3000
```

Stop it with the VS Code task `Stop VIA Tabloid locally`, or run:

```bash
docker compose down
```
